Spin up a team of usability tester agents to test every usability path as described in paths/. There should be one usability tester agent per path. Each usability agent should use it's own playwright-cli session and survey sandbox. 

Before beginning, ask for the Anthropic API key that you can give to each usability tester agent. Tell each usability agent to use the "/usability" slash command skill.
